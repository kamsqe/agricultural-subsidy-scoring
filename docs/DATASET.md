# Описание данных

---

## 1. Основной датасет (предоставлен организаторами)

Источник: subsidy.plem.kz (ГИСС). Выгрузка 19.03.2026. Формат: Excel (.xlsx).

- **36,651 записей** (заявок)
- **11 колонок** (+ пустые колонки 3, 4, 14-17)
- Период: январь 2025 — март 2026

### Колонки

| # | Название | Тип | Пример |
|---|---|---|---|
| 1 | № п/п | int | 58358 |
| 2 | Дата поступления | datetime | 21.01.2025 11:15:40 |
| 5 | Область | str (18 значений) | область Абай |
| 6 | Акимат | str | ГУ "Управление сельского хозяйства..." |
| 7 | Номер заявки | str (14 цифр) | 01300100258072 |
| 8 | Направление | str (9 значений) | Субсидирование в скотоводстве |
| 9 | Наименование субсидирования | str (~40 типов) | Заявка на получение субсидий на... |
| 10 | Статус заявки | str (6 значений) | Исполнена |
| 11 | Норматив | int/float | 15000 |
| 12 | Причитающая сумма | int/float | 4635000 |
| 13 | Район хозяйства | str (192 значения) | Жарминский район |

### Статусы заявок

| Статус | Количество | % | Значение |
|---|---|---|---|
| Исполнена | 21,012 | 57.3% | Субсидия выплачена |
| Одобрена | 7,615 | 20.8% | Одобрена, ожидает выплаты |
| Отклонена | 2,909 | 7.9% | Отказ |
| Сформировано поручение | 2,854 | 7.8% | Одобрена, ждёт бюджет |
| Отозвано | 2,064 | 5.6% | Фермер отозвал сам |
| Получена | 197 | 0.5% | В процессе рассмотрения |

### Структура номера заявки

```
01300 100 258072
│     │   │
│     │   └── Последовательный номер
│     └────── Код подтипа (1003, 1002, 0003...)
└──────────── Код типа субсидии (46 уникальных)
```

### Ключевые числа

- Общая сумма: **139.3 млрд тенге**
- Средняя заявка: 3.8 млн тенге, медиана: 1.0 млн тенге
- 18 областей, 193 района, 46 типов субсидий
- 9 направлений: скотоводство (67%), птицеводство (15%), овцеводство (10%)

---

## 2. Внешние источники (собраны самостоятельно)

### stat.gov.kz — региональная статистика

**Скрипт:** `data/external/analyze_stat_gov.py`
**Файлы:** `data/external/stat_gov_kz/` (70 xlsx)
**Выход:** `data/external/regional_features.csv` — 20 регионов × 34 признака

| Категория | Файлы | Что содержит |
|-----------|-------|-------------|
| Поголовье по регионам | element_458518.xlsx (12 листов) | КРС, овцы, козы, лошади, верблюды, свиньи, птица, молоко, мясо — 1990-2024, 20 регионов |
| Размер ферм | element_440925, 440926 | Распределение ферм по размеру (мелкие/средние/крупные) по регионам |
| Месячная динамика | elements 8125-8131 | Ежемесячное поголовье 2019-2025 по регионам |

**34 признака на регион:** поголовье по видам (2020-2024), доля мелких/средних ферм, месячные тренды (YoY %), производство молока и мяса.

### subsidy.plem.kz — API субсидий

**Скрипт:** `data/external/download_plem.py`

| Endpoint | Метод | Что возвращает | Файл |
|----------|-------|---------------|------|
| `Default.aspx/GetYearBudget2020` | POST | Бюджет по 18 областям (117.5 млрд ₸) | `budget_by_region.csv` |
| `Default.aspx/GetStatistic2020` | POST | Воронка одобрения по области × тип субсидии (436 строк) | `statistics_by_region.csv` |
| `Default.aspx/GetCountEnterprise` | POST | 39,193 зарегистрированных предприятий | `plem_api_raw.json` |
| `MainStatic.aspx/GetRegistrApp` | POST | Реестры заявок 2019-2024 (429K записей) | `registry_*.csv` |

**Поля реестров:** RowNum, SendDate, State, Enterprise, BidNumber, BidStatus, SubsidiesName, AkimatUshName, SubsidiesOwedSum, Solution.

### Не использованные источники

| Источник | Причина |
|----------|---------|
| data.egov.kz | Требует API-ключ |
| gosagro.kz | Angular SPA + ЭЦП-авторизация |

---

## 3. Обогащённый датасет

**Pipeline:** `pipeline/build_features.py`
**Выход:** `data/processed/enriched_applications.csv` — 36,651 строк × 67 столбцов

### Производные признаки (из основного датасета)

| Признак | Формула | Описание |
|---------|---------|----------|
| `volume` | amount / norm | Объём продукции (единицы зависят от типа!) |
| `subsidy_code` | app_number[:5] | Код типа субсидии |
| `category` | lookup by code | acquisition / production / breeding / emergency / other |
| `retry_count` | case-level dedup | Номер попытки (0 = первая) |
| `is_retry` | retry_count > 0 | Повторная подача |
| `month`, `day_of_week`, `hour` | из даты | Временные признаки |

### Агрегатные признаки (из основного датасета)

| Признак | Уровень | Описание |
|---------|---------|----------|
| `district_reject_rate` | район | Доля отклонённых заявок |
| `district_top1_share` | район | Доля крупнейшего получателя (monopoly index) |
| `district_median_amount` | район | Медианная сумма заявки |
| `amount_vs_median` | заявка | Сумма / медиана района |
| `oblast_backlog_ratio` | область | Доля заявок в статусе «ожидание бюджета» |
| `oblast_execution_rate` | область | Доля исполненных заявок |
| `type_reject_rate` | тип субсидии | Доля отклонённых по типу |
| `is_round_million` | заявка | Сумма кратна 1 млн |
| `is_weekend` | заявка | Подача в выходной |

### Внешние признаки (из stat.gov.kz)

26 признаков с префиксом `reg_`: поголовье по видам, производство молока и мяса, доля мелких ферм, месячные тренды.

### Внешние признаки (из plem.kz)

| Признак | Описание |
|---------|----------|
| `oblast_budget` | Бюджет области (тенге) |
| `oblast_applicants` | Число активных заявителей |
| `oblast_enterprises` | Число зарегистрированных предприятий |
| `budget_per_applicant` | Бюджет / число заявителей |
| `oblast_approval_rate` | Доля одобренных (из внешней воронки) |
| `oblast_execution_rate_ext` | Доля исполненных (из внешней воронки) |

**Сравнивать production_volume между разными нормативами нельзя!** Единицы: головы (150K-260K ₸/голова), кг (20-300 ₸/кг), дозы (10K ₸/доза).

---

## 4. Синтетические данные (сгенерированы)

Используются **только** в Проактивном модуле (`ProactiveFinder.tsx`). Остальные модули работают на реальных данных.

### Профили фермеров

**Файл:** `agroscore/public/data/farmer_profiles.json` — 200 записей
**Генератор:** `generate_profiles.py`

| Поле | Тип | Откуда распределения |
|------|-----|---------------------|
| `id`, `name` | str | Рандомизированы |
| `oblast`, `district` | str | Реальные названия из основного датасета |
| `cattle`, `sheep`, `goats`, `horses`, `poultry` | int | Распределения из stat.gov.kz (поголовье по регионам) |
| `land_ha`, `pasture_ha` | float | Распределения из stat.gov.kz (земельный фонд) |
| `milking_cows`, `daily_milk_liters` | int/float | Оценка из stat.gov.kz (молоко по регионам) |
| `has_milking_equipment`, `has_feed_storage` | bool | Случайные, по оценке распространённости |
| `feed_tons`, `housing_capacity` | float | Пропорционально поголовью |
| `mortality_rate` | float | 1-8% (норма КЗ: 3% КРС, 5% МРС) |
| `credit_history` | str | good/fair/poor (рандом) |
| `subsidy_history_count` | int | 0-5 (рандом) |

**Важно:** Это не реальные фермеры. Профили сгенерированы программно. Распределения приближены к реальным, но конкретные комбинации — случайные.

### Агронормы

**Файл:** `agroscore/public/data/agro_norms.json`

| Норма | Значение | Источник |
|-------|---------|---------|
| Пастбищная нагрузка КРС | 3.0 га/голова (степь 4.0, орошаемые 1.8) | Нормативы МСХ РК |
| Пастбищная нагрузка МРС | 0.5 га/голова | Нормативы МСХ РК |
| Падёж КРС (норма) | ≤3% | Ветеринарные нормативы |
| Падёж МРС (норма) | ≤5% | Ветеринарные нормативы |
| Обеспеченность помещениями | ≥80% | Зоотехнические нормативы |
| Корма на зиму (КРС) | 3.0 т/голову | Кормовые нормативы |

**Значения реальные** (из нормативных документов), структура JSON — наша.

### Коды субсидий

**Файл:** `agroscore/public/data/subsidy_codes.json`

Статистика по 46 типам субсидий — **вычислена из реального основного датасета** (средние, медианы, count).

### Районы

**Файл:** `agroscore/public/data/districts.json`

Агрегаты по 192 районам — **вычислены из реального основного датасета** (reject rate, top1 share, median amount, avg score).

---

## 5. ML-выходы

| Файл | Модель | Данные | Что содержит |
|------|--------|--------|-------------|
| `data/ml_outputs/anomaly_scores.csv` | Isolation Forest (200 деревьев, contamination=2%) | 8 поведенческих признаков из enriched dataset | anomaly_score (0-100) для 36,651 заявок |
| `pipeline/models/iso_forest_pipeline.pkl` | Isolation Forest | Сериализованный pipeline (imputer + scaler + model) | Для инференса на новых данных |
| `pipeline/models/xgb_bias_discovery.json` | XGBoost (150 деревьев, depth=5) | 13 признаков, target=Отклонена | Модель bias discovery (**не для скоринга**) |
| `data/processed/shap_importance.json` | SHAP (TreeExplainer) | Sample 5,000 из XGBoost | Mean |SHAP| по 13 признакам |

**XGBoost + SHAP используется только для визуализации.** Он доказывает, что исторические отказы определяются geography/budget, а не качеством заявки. Это аргумент в пользу Impact Scoring вместо prediction.

---

## 6. Сводная карта данных

| Компонент системы | Какие данные использует | Тип данных |
|-------------------|------------------------|-----------|
| Impact Score (36K заявок) | Основной датасет + stat.gov.kz + plem.kz | **Реальные** |
| Anomaly Radar | anomaly_scores.csv (Isolation Forest) | **ML на реальных данных** |
| FIFO vs Merit | 2,854 заявки в очереди | **Реальные** (порядок моделируемый) |
| SHAP Bias | XGBoost на исторических отказах | **ML на реальных данных** |
| Pre-Check (UI) | scoringEngine.ts + districts + codes | **Реальные агрегаты** (ввод пользователя) |
| Rejection Reasons | plem.kz 2021/2024, поле Solution | **Реальные** |
| Proactive Finder | farmer_profiles.json + agro_norms.json | **Синтетические профили + реальные нормативы** |
| AI Assistant (Gemini) | scoring_summary.json + user query | **Реальные агрегаты + генеративный AI** |
